variables:
  PREFIX: "riscv64-unknown-elf-"
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - test

exec_test:
  stage: test
  image: jiegec/meowv64
  script:
    - cd testcases
    - make
    - cd ..
    - mill mewov64.test.test
